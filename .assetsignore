# === .assetsignore 文件说明 ===
# 这个文件告诉 Cloudflare Wrangler CLI 在部署 Worker 的静态资源 (assets) 时，哪些文件或目录应该被忽略，不会被上传。
# 这对于避免上传不必要的或敏感的文件非常重要。
# 文件中的每一行都是一个模式，匹配到的文件或目录将被忽略。
# 模式的语法类似于 .gitignore。

# 忽略 Wrangler 的本地开发和构建相关的目录
.wrangler

# 忽略项目依赖目录，这些依赖通常在构建时处理，不需要作为静态资源上传
node_modules

# 忽略 Git 版本控制目录
.git

# 忽略包含本地开发环境变量的文件，这是敏感信息，绝不应上传
.dev.vars

# 忽略 npm 的包锁定文件，它用于确保依赖版本一致性，但不需要作为静态资源上传
package-lock.json